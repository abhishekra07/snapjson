import React from "react";
import { Typography, Box } from "@mui/material";

const JsonToYaml = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">JSON to YAML (Coming Soon)</Typography>
    </Box>
  );
};

export default JsonToYaml;
