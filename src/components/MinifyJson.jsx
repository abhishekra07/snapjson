import React from "react";
import { Typography, Box } from "@mui/material";

const MinifyJson = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Minify JSON (Coming Soon)</Typography>
    </Box>
  );
};

export default MinifyJson;
